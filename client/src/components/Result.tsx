import type { Analysis } from '../types';
const labels = (score:number) => score >= 90 ? 'Excellent Match' : score >= 75 ? 'Good Match' : score >= 50 ? 'Moderate Match' : 'Low Match';
function Skills({title, items, kind='neutral'}:{title:string;items:string[];kind?:'good'|'bad'|'neutral'}) { const style = kind === 'good' ? 'bg-emerald-50 text-emerald-700' : kind === 'bad' ? 'bg-rose-50 text-rose-700' : 'bg-slate-100 text-slate-700'; return <section><h3 className="mb-3 font-semibold">{title}</h3><div className="flex flex-wrap gap-2">{items.length ? items.map(x=><span key={x} className={`rounded-full px-3 py-1 text-sm ${style}`}>{kind==='good'?'✓ ':kind==='bad'?'× ':''}{x}</span>) : <span className="text-sm text-slate-400">None detected</span>}</div></section> }
export function Result({analysis}:{analysis:Analysis}) { return <div className="mt-10 space-y-7 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col items-center justify-center border-b border-slate-100 pb-7"><div className="relative grid h-32 w-32 place-items-center rounded-full" style={{background:`conic-gradient(#4f46e5 ${analysis.score}%, #e2e8f0 0)`}}><div className="grid h-24 w-24 place-items-center rounded-full bg-white text-center"><div><strong className="text-3xl">{analysis.score}%</strong><div className="text-xs text-slate-500">Match Score</div></div></div></div><p className="mt-3 font-semibold text-indigo-700">{labels(analysis.score)}</p></div><div className="grid gap-7 md:grid-cols-2"><Skills title="Matched Skills" items={analysis.matchedSkills} kind="good"/><Skills title="Missing Skills" items={analysis.missingSkills} kind="bad"/><Skills title="Resume Skills" items={analysis.resumeSkills}/><Skills title="Job Skills" items={analysis.jobSkills}/></div><section><h3 className="mb-3 font-semibold">Suggestions</h3><ul className="space-y-2 text-sm text-slate-600">{analysis.suggestions.map(x=><li key={x} className="rounded-lg bg-indigo-50 p-3">{x}</li>)}</ul></section></div>; }

