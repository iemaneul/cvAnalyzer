import { Link } from 'react-router-dom'; import { useAnalyses } from '../hooks/analyses';
export function History(){const {data,isLoading,error}=useAnalyses();return <div><h1 className="text-3xl font-bold">Analysis history</h1><p className="mt-2 text-slate-500">Review your previous resume matches.</p><div className="mt-7 overflow-hidden rounded-xl border bg-white">{isLoading?<p className="p-6">Loading...</p>:error?<p className="p-6 text-rose-600">Unable to load analyses.</p>:!data?.length?<p className="p-10 text-center text-slate-500">No analyses yet.</p>:<table className="w-full text-left"><thead className="bg-slate-50 text-sm text-slate-500"><tr><th className="p-4">Resume</th><th className="p-4">Score</th><th className="p-4">Date</th></tr></thead><tbody>{data.map(x=><tr key={x.id} className="border-t hover:bg-slate-50"><td className="p-4"><Link className="font-medium text-indigo-700" to={`/history/${x.id}`}>{x.fileName}</Link></td><td className="p-4 font-semibold">{x.score}%</td><td className="p-4 text-slate-500">{new Intl.DateTimeFormat('en',{dateStyle:'medium'}).format(new Date(x.createdAt))}</td></tr>)}</tbody></table>}</div></div>}

